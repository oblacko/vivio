# Деплой Vivio на GitHub

Этот документ описывает процесс настройки автоматического деплоя приложения Vivio на Vercel через GitHub Actions.

## 🚀 Настройка деплоя

### 1. Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите "New repository"
3. Укажите название репозитория (например, `vivio-app`)
4. Не добавляйте README, .gitignore или лицензию (они уже есть в проекте)
5. Нажмите "Create repository"

### 2. Подключение локального репозитория к GitHub

```bash
# Добавьте remote origin (замените YOUR_USERNAME и YOUR_REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# Отправьте код на GitHub
git push -u origin master
```

### 3. Настройка секретов в GitHub

#### Получение токена Vercel:

1. Перейдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Перейдите в Account Settings → Tokens
3. Создайте новый токен с именем "GitHub Actions"
4. Скопируйте токен

#### Получение Vercel Project ID и Org ID:

```bash
# Установите Vercel CLI
npm install -g vercel

# Авторизуйтесь
vercel login

# Перейдите в папку проекта
cd /path/to/your/project

# Свяжите проект с Vercel (если еще не связано)
vercel link

# Получите информацию о проекте
vercel project ls
```

Или найдите эти значения в файле `.vercel/project.json` в корне проекта.

#### Добавление секретов в GitHub:

1. Перейдите в ваш репозиторий на GitHub
2. Перейдите в Settings → Secrets and variables → Actions
3. Нажмите "New repository secret" и добавьте:

- `VERCEL_TOKEN` - ваш токен Vercel
- `VERCEL_ORG_ID` - ID вашей организации Vercel
- `VERCEL_PROJECT_ID` - ID проекта Vercel

### 4. Настройка переменных окружения

Создайте файл `.env.local` на основе `env.template` и настройте все необходимые переменные:

```bash
cp env.template .env.local
```

Заполните все переменные в `.env.local` актуальными значениями.

## 🔄 Процесс деплоя

### Автоматический деплой

После настройки секретов, деплой будет происходить автоматически:

- **Push в main ветку** → Продакшн деплой на Vercel
- **Pull Request в main** → Preview деплой на Vercel
- **Любой push/PR** → Проверка линтера

### Ручной деплой

Вы также можете запустить деплой вручную:

1. Перейдите во вкладку "Actions" в вашем репозитории
2. Выберите workflow "Deploy to Vercel"
3. Нажмите "Run workflow"

## 📋 Требования

### Переменные окружения

Убедитесь, что в вашем проекте настроены следующие переменные:

- `DATABASE_URL` - URL базы данных PostgreSQL
- `REDIS_URL` - URL Redis для кэширования
- `GROK_API_KEY` - API ключ для Grok AI
- `VERCEL_BLOB_READ_WRITE_TOKEN` - токен для Vercel Blob storage
- `AWS_ACCESS_KEY_ID` и `AWS_SECRET_ACCESS_KEY` - ключи AWS (если используете S3)

### Секреты GitHub

- `VERCEL_TOKEN`
- `VERCEL_ORG_ID`
- `VERCEL_PROJECT_ID`

## 🔍 Мониторинг деплоев

### Проверка статуса

1. Перейдите во вкладку "Actions" в репозитории
2. Просмотрите статус последних workflow'ов
3. При ошибках проверьте логи для диагностики

### Vercel Dashboard

1. Перейдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Просмотрите deployments и логи

## 🛠 Troubleshooting

### Распространенные проблемы

1. **Ошибка авторизации Vercel**
   - Проверьте правильность `VERCEL_TOKEN`
   - Убедитесь, что токен не истек

2. **Ошибка сборки**
   - Проверьте переменные окружения
   - Убедитесь, что база данных доступна
   - Проверьте логи сборки в Actions

3. **Проблемы с Prisma**
   - Запустите `npx prisma generate` локально
   - Проверьте `DATABASE_URL`

4. **Проблемы с зависимостями**
   - Очистите node_modules: `rm -rf node_modules && npm install`
   - Проверьте package-lock.json

### Логи и отладка

- **GitHub Actions**: Перейдите в Actions → Выберите workflow → Просмотрите логи
- **Vercel**: Dashboard → Deployments → Выберите deployment → View logs

## 📞 Поддержка

Если возникли проблемы с настройкой деплоя:

1. Проверьте этот документ еще раз
2. Посмотрите логи в GitHub Actions
3. Проверьте Vercel Dashboard
4. Создайте issue в репозитории с подробным описанием проблемы