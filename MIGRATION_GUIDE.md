# Руководство по миграции БД - Vivio

## Обзор изменений

В этом обновлении были добавлены следующие изменения в схему базы данных:

1. **UserRole enum** - для разграничения прав доступа (USER, ADMIN)
2. **Поле `role`** в модели User - роль пользователя (по умолчанию USER)
3. **Поле `bio`** в модели User - описание пользователя (опционально, до 500 символов)

## Шаги миграции

### 1. Создание миграции

Выполните команду для создания новой миграции:

```bash
npx prisma migrate dev --name add_user_role_and_bio
```

Эта команда:
- Создаст новый файл миграции в `prisma/migrations/`
- Применит изменения к базе данных разработки
- Сгенерирует обновленный Prisma Client

### 2. Проверка миграции

Откройте созданный файл миграции в папке `prisma/migrations/` и убедитесь, что SQL выглядит примерно так:

```sql
-- CreateEnum
CREATE TYPE "UserRole" AS ENUM ('USER', 'ADMIN');

-- AlterTable
ALTER TABLE "users" ADD COLUMN "role" "UserRole" NOT NULL DEFAULT 'USER',
ADD COLUMN "bio" TEXT;
```

### 3. Применение миграции к production

Для применения миграции к production базе данных:

```bash
npx prisma migrate deploy
```

### 4. Создание первого администратора

После применения миграции необходимо назначить роль администратора хотя бы одному пользователю:

#### Через Prisma Studio:

```bash
npx prisma studio
```

1. Откройте таблицу `users`
2. Найдите нужного пользователя
3. Измените поле `role` с `USER` на `ADMIN`
4. Сохраните изменения

#### Через SQL запрос:

```sql
UPDATE users 
SET role = 'ADMIN' 
WHERE email = 'your-admin-email@example.com';
```

#### Через скрипт Node.js:

Создайте файл `scripts/create-admin.ts`:

```typescript
import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

async function main() {
  const email = process.argv[2];
  
  if (!email) {
    console.error("Usage: npm run create-admin <email>");
    process.exit(1);
  }

  const user = await prisma.user.update({
    where: { email },
    data: { role: "ADMIN" },
  });

  console.log(`User ${user.email} is now an admin!`);
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

Выполните:

```bash
npx tsx scripts/create-admin.ts your-admin-email@example.com
```

## Откат изменений (если необходимо)

Если нужно откатить изменения:

```bash
npx prisma migrate reset
```

**⚠️ ВНИМАНИЕ:** Эта команда удалит все данные из базы данных!

## Проверка изменений

После миграции проверьте:

1. ✅ Все существующие пользователи имеют роль `USER` по умолчанию
2. ✅ Поле `bio` добавлено и равно `NULL` для существующих пользователей
3. ✅ Хотя бы один пользователь имеет роль `ADMIN`
4. ✅ Админ-панель доступна по адресу `/admin` для администраторов
5. ✅ Редактирование профиля работает корректно

## Новые возможности после миграции

### Для администраторов:
- 📊 Доступ к админ-панели (`/admin`)
- ✏️ Создание новых челленджей
- 📝 Редактирование существующих челленджей
- 🗑️ Деактивация челленджей

### Для всех пользователей:
- 👤 Страница профиля с аватаром, био и социальными сетями
- ✏️ Редактирование профиля
- 🖼️ Загрузка аватара
- 🔒 Настройка приватности профиля
- 📱 Добавление ссылок на социальные сети

## Переменные окружения

Убедитесь, что в `.env` есть все необходимые переменные:

```env
# Database
POSTGRES_URL=
POSTGRES_PRISMA_URL=

# Vercel Blob для хранения аватаров
BLOB_READ_WRITE_TOKEN=

# NextAuth
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
```

## Troubleshooting

### Ошибка: "Type 'UserRole' does not exist"

Решение:
```bash
npx prisma generate
npx prisma db push
```

### Ошибка: "Column 'role' cannot be null"

Это означает, что миграция не применилась корректно. Проверьте:
1. Подключение к базе данных
2. Права пользователя БД
3. Логи миграции

### Невозможно создать администратора

Убедитесь, что:
1. Пользователь существует в БД
2. Email указан правильно (с учетом регистра)
3. У вас есть права на UPDATE в таблице users

## Поддержка

Если возникли проблемы с миграцией, проверьте:
- Логи Prisma: `npx prisma migrate status`
- Текущую схему БД: `npx prisma db pull`
- Версию Prisma: `npx prisma --version`

---

**Дата создания:** 2026-01-17
**Версия приложения:** v2.0.0
