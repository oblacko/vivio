# Генератор Вайбов с DeepSeek AI

## Описание

Конвейер для автоматической генерации вайбов с помощью DeepSeek AI. Система принимает текстовые инструкции, генерирует структурированный список вайбов через AI и сохраняет их как неопубликованные для последующей модерации.

## Возможности

- ✅ Генерация вайбов через DeepSeek AI на основе текстовых инструкций
- ✅ Поддержка JSON Schema для структурированного вывода
- ✅ Валидация сгенерированных вайбов перед сохранением
- ✅ Сохранение вайбов как неактивных (требуют модерации)
- ✅ Модерация через админ-панель (активация/деактивация)
- ✅ Обработка дубликатов и ошибок

## Установка

### 1. Получите API ключ DeepSeek

Зарегистрируйтесь на [DeepSeek](https://platform.deepseek.com/) и получите API ключ.

### 2. Добавьте переменные окружения

В файл `.env.local` добавьте:

```env
DEEPSEEK_API_KEY="your-deepseek-api-key-here"
DEEPSEEK_API_URL="https://api.deepseek.com"
```

### 3. Установите зависимости (если нужно)

```bash
npm install
```

## Использование

### Через админ-панель

1. Войдите в админ-панель: `/admin`
2. Перейдите в раздел "Генерация AI"
3. Введите инструкцию для AI (например: "Создай 5 вайбов на тему природы и животных")
4. Укажите количество вайбов (опционально)
5. Нажмите "Сгенерировать вайбы"
6. Проверьте результаты генерации

### Модерация вайбов

1. Перейдите в раздел "Вайбы"
2. Найдите сгенерированные вайбы (помечены как "Неактивен")
3. Нажмите "Активировать" для публикации вайба
4. Или нажмите "Редактировать" для изменения перед публикацией

### Через API

**POST** `/api/admin/vibes/generate`

Требуется роль: `ADMIN`

**Request Body:**
```json
{
  "instruction": "Создай 5 креативных вайбов на тему космоса",
  "count": 5,
  "jsonSchema": "optional custom JSON schema"
}
```

**Response:**
```json
{
  "success": true,
  "created": 5,
  "total": 5,
  "vibes": [
    {
      "id": "...",
      "title": "Космическое путешествие",
      "category": "SEASONAL",
      "promptTemplate": "Transform into a cosmic journey...",
      "description": "...",
      "isActive": false
    }
  ],
  "errors": []
}
```

## Структура вайба

Каждый сгенерированный вайб содержит:

- `title` (string, обязательно) - Уникальное название на русском
- `category` (enum, обязательно) - MONUMENTS | PETS | FACES | SEASONAL
- `promptTemplate` (string, обязательно) - Промпт для AI генерации видео
- `description` (string, опционально) - Описание вайба

## Архитектура

```
Админ UI (VibeGenerator)
    ↓
POST /api/admin/vibes/generate
    ↓
DeepSeek Client
    ↓
DeepSeek API (с JSON Schema)
    ↓
Валидация (Zod)
    ↓
Сохранение в БД (isActive: false)
    ↓
Модерация (Админ панель)
    ↓
PATCH /api/admin/vibes/[id]
    ↓
Публикация (isActive: true)
```

## Компоненты

### Backend

- `src/lib/deepseek/client.ts` - Клиент для DeepSeek API
- `src/lib/deepseek/vibe-schema.ts` - Схемы валидации и JSON Schema
- `src/app/api/admin/vibes/generate/route.ts` - API endpoint для генерации
- `src/app/api/admin/vibes/[id]/route.ts` - API endpoint для модерации

### Frontend

- `src/components/admin/VibeGenerator.tsx` - UI компонент для генерации
- `src/app/admin/page.tsx` - Админ-панель с секциями

## Примеры инструкций

### Базовый пример
```
Сгенерируй 5 креативных вайбов для видео-челленджа
```

### С конкретной темой
```
Создай 10 вайбов на тему памятников и архитектуры разных стран
```

### С детализацией
```
Сгенерируй 7 вайбов:
- 3 на тему домашних животных
- 2 на тему сезонов года
- 2 на тему известных монументов

Каждый вайб должен быть креативным и подходить для AI генерации видео
```

## Обработка ошибок

Система автоматически обрабатывает:

- ✅ Дубликаты названий (пропускает с предупреждением)
- ✅ Невалидные категории (отклоняет)
- ✅ Ошибки API (логирует и возвращает детали)
- ✅ Ошибки валидации (возвращает детали)

## Логирование

Все операции логируются:

```
🎨 Запуск генерации вайбов
📝 Instruction: ...
🔢 Count: 5
🚀 Отправка запроса в DeepSeek API
✅ DeepSeek вернул 5 вайбов
✅ Вайб "..." создан с ID: ...
📊 Итого создано вайбов: 5/5
```

## Модели DeepSeek

По умолчанию используется модель `deepseek-chat` с параметрами:

- `temperature`: 0.8 (креативность)
- `max_tokens`: 4000
- `response_format`: { type: "json_object" }

## Ограничения

- Максимум 50 вайбов за один запрос
- Требуется валидный API ключ DeepSeek
- Требуется роль ADMIN для доступа к API

## Безопасность

- ✅ Проверка авторизации и роли
- ✅ Валидация всех входных данных
- ✅ Проверка на дубликаты
- ✅ Санитизация данных перед сохранением
- ✅ Все вайбы создаются неактивными

## Troubleshooting

### Ошибка "DEEPSEEK_API_KEY is not configured"

Убедитесь, что добавили API ключ в `.env.local`

### Вайбы не генерируются

1. Проверьте API ключ
2. Проверьте баланс на счету DeepSeek
3. Проверьте логи в консоли браузера и сервера

### Вайб не активируется

Убедитесь, что:
1. У вас роль ADMIN
2. Вайб существует в БД
3. Нет ошибок в консоли

## Поддержка

При возникновении проблем проверьте:
1. Логи сервера (console.log)
2. Network tab в DevTools
3. Наличие всех необходимых переменных окружения
